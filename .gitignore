# The two private trees, never committed. Never `git add -f` anything under either.
#   private_web_spice_models/  vendor model text and symbols: what the site shows in private mode
#   private_material/          OCR, its map, and everything still in transit
private_web_spice_models/
private_material/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Web
node_modules/
web/dist/
# built by `make web-data` from data/; never committed
web/public/data/

# Simulator output
*.raw
*.op.raw
*.log
*.net.bak

# Secrets and local config
.env
.env.*
config.toml
*_key.txt

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
